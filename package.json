{
  "name": "blamely",
  "engines": {
    "node": "10.4.1",
    "yarn": "1.3.2"
  },
  "scripts": {
    "build": "npm run --cwd client install && npm run --cwd client build",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "npm run build && npm run deploy"
  }
}